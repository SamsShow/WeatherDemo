// Swift
import Foundation

// Use the app's canonical model type for previews to avoid type mismatches.
// Remove the old struct ResponseBody and replace with this alias.
typealias ResponseBody = WeatherManager.ResponseBody
